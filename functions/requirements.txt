# Core audio processing libraries
soundfile
numpy

# Google Cloud services
google-cloud-storage
firebase-admin
functions-framework

# Scientific computing
scipy

# Testing and development
pytest
pytest-cov

# Performance monitoring (optional)
psutil

# Note: parselmouth will be installed separately if needed
# The function has fallback mock implementation if parselmouth is unavailable